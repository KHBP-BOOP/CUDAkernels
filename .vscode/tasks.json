{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "CUDA: nvcc build active file",
            "command": "/usr/bin/nvcc", 
            "args": [
                "-g",
                "-G",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "CUDA compiler task generated for NVCC"
        }
    ],
    "version": "2.0.0"
}